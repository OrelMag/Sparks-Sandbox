/**
 * SkillTier.ts — how hard Phase 4 gets.
 *
 * Chosen by the child by walking Spark through one of two doors in the hub
 * (never a labeled menu). SIMPLE keeps everything a toy: pressure plates and
 * single switches. LOGIC switches on AND/OR/NOT layers and draws the circuitry
 * exposed. Same rooms, same engine — an extra logical layer, not a different
 * game. Nothing silently judges the player.
 */

export enum SkillTier {
  SIMPLE = "SIMPLE",
  LOGIC = "LOGIC",
}
